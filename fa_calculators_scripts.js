jQuery(document).ready(function(){
    //check that it's loading
    console.log("ready");
    jQuery("#submit").click(function(){
        event.preventDefault();
        assets = jQuery(this).attr
        liabilities = 
        current_ratio = 
    });
});
